# Tory Adams Wears — Demo Ecommerce App

Run locally:

1. Extract the project folder.
2. In the project root, run:
   ```
   npm install
   npm run dev
   ```
3. Open the dev server URL shown by Vite (usually http://localhost:5173).

Notes:
- Uses DummyJSON (https://dummyjson.com) for product data.
- Cart is stored in localStorage.
- Contact form saves submissions to localStorage (no external email).
